<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.ryan.env.mapper.stock.GoodsMapper">
    <select id="queryById" parameterType="java.lang.Long" resultType="com.ryan.env.model.Goods">
        select * from goods where id=#{id}
    </select>
</mapper>