<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ryan.env.mapper.act.UserMapper">
    <select id="getAllUser" resultType="com.ryan.env.model.User">
        select * from user;
    </select>
    <insert id="addUser" parameterType="com.ryan.env.model.User">
        insert into user (username,address) values (#{username},#{address});
    </insert>
    <update id="updateUserById" parameterType="com.ryan.env.model.User">
        update user set username=#{username},address=#{address} where id=#{id}
    </update>
    <delete id="deleteUserById">
        delete from user where id=#{id}
    </delete>
    <select id="getUserById" parameterType="java.lang.Long" resultType="com.ryan.env.model.User">
        select * from user where id=#{id}
    </select>
</mapper>